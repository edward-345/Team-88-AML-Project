# Input

Place the **single pipeline input** here:

- **master_features_with_clustering.csv** — Produced by **clean_data** (see `AML_Submission/clean_data/`). One row per customer; all feature columns + **cluster** (string labels).

From submission root: run the clean_data pipeline (see `clean_data/README.md`); then copy `clean_data/features/final/master_features_with_clustering.csv` here.
